---
sudo: false
language: ruby
cache: bundler
rvm:
  - 2.6.6
before_install: 
  - gem install bundler
  - gem install rspec
  - gem install rubocop
  - gem install dry-struct
  - gem install faraday
  - gem install pry
  
install: true
script:
  - bundle exec rake rubocop
  - bundle exec rake spec
